name: inventory_gudang
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

dependencies:
  args: ^2.4.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  dart_jsonwebtoken: ^2.14.1
  web_socket_channel: ^3.0.1
  mongo_pool: ^1.4.3
  

dev_dependencies:
  http: ^1.1.0
  lints: ^4.0.0
  test: ^1.24.0
